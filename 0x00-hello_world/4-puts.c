#include <stdio.h>

/**
 * main - Entry point
 *
 *Description: c program that used 'puts' to print out a string
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
